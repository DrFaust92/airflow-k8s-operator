apiVersion: airflow.drfaust92/v1beta1
kind: Variable
metadata:
  name: broken-variable
  # namespace: default
spec:
  secretRef:
    name: this-secret-does-not-exist
    key: whatever
  description: "Variable whose secretRef points at a nonexistent Secret (failure case)."
