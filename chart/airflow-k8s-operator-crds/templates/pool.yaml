# Generated by scripts/gen_crds.py from resources/schemas.py -- DO NOT EDIT.
# Run `make generate` after changing a spec model.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: pools.airflow.drfaust92
  annotations:
    helm.sh/resource-policy: keep
spec:
  group: airflow.drfaust92
  scope: Namespaced
  names:
    kind: Pool
    plural: pools
    singular: pool
    shortNames:
    - pls
    - pl
  versions:
  - name: v1beta1
    served: true
    storage: true
    subresources:
      status: {}
    additionalPrinterColumns:
    - name: Phase
      type: string
      jsonPath: .status.phase
    - name: Message
      type: string
      jsonPath: .status.message
    schema:
      openAPIV3Schema:
        type: object
        properties:
          spec:
            properties:
              slots:
                description: The maximum number of slots that can be assigned to tasks. One job may occupy one or more slots.
                type: integer
              includeDeferred:
                description: Whether to include deferred tasks in the pool.
                type: boolean
              description:
                type: string
                description: The pool description.
              teamName:
                type: string
                description: Team for Airflow 3 multi-team mode. Requires multi-team mode enabled and the team to exist in Airflow, otherwise the API may reject the resource. Ignored on Airflow 2.
            required:
            - slots
            type: object
          status:
            type: object
            x-kubernetes-preserve-unknown-fields: true
            properties:
              phase:
                type: string
                description: Last reconciliation outcome (Synced or Error).
              message:
                type: string
                description: Human-readable detail about the last reconciliation.
