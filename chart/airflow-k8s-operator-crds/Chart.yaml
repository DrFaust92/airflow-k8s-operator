apiVersion: v2
name: airflow-k8s-operator-crds
description: |
  CustomResourceDefinitions for the Airflow Kubernetes Operator
  (Connection, Pool, Variable). Cluster-scoped and shared: install once, then
  deploy the operator chart with crds.create=false for multi-tenant setups.
type: application

# Chart version (Semantic Versioning). Bump when a CRD changes.
version: 0.1.0

# The operator app version these CRDs correspond to.
appVersion: "0.6.0"

maintainers:
  - name: DrFaust92
    url: https://github.com/DrFaust92
  - name: KyriosGN0
    url: https://github.com/KyriosGN0
